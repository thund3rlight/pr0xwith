{
  "name": "pr0xwith",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 migrations apply proxy_dashboard --local",
    "db:init:remote": "wrangler d1 migrations apply proxy_dashboard --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
